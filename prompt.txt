# Correction Run 3 — Equity Card + Copy Improvements

## CRITICAL CONSTRAINT
Only modify `src/components/quiz/FundingRmResultsCard.tsx`.
Do not modify any other file.
Do not change any logic, state variables, or Supabase submission code.
Do not touch SNF or HCA tracks.

---

## Read First
Read `src/components/quiz/FundingRmResultsCard.tsx` completely before
making any changes.

---

## Change 1 — Show Only the Relevant Age Band Card

The current component renders all three equity band cards pre-gate.
Replace this with a single card showing only the band matching
`result.ageBand`.

Band index mapping:
- ageBand 1 → equityIllustration[0] (Age 62-69)
- ageBand 2 → equityIllustration[1] (Age 70-79)
- ageBand 3 → equityIllustration[2] (Age 80 or older)

If ageBand is 0 (not-a-fit) or equityIllustration is empty,
do not render this section at all.

The single card uses the same styling as the existing cards:
`rounded-xl border border-stone-100 bg-white px-5 py-4`

---

## Change 2 — Add Personalizer Line Above the Card

Directly above the single equity card, add:

```tsx
<p className="text-[14px] text-stone-500 mb-3">
  Based on the age range you indicated:
</p>
```

---

## Change 3 — Add Label Above the Blurred Number

Inside the equity card, directly above the rangeLabel element, add:

```tsx
<p className="text-[13px] font-mono uppercase tracking-widest
  text-stone-400 mb-1">
  Estimated equity available:
</p>
```

This label must appear BOTH in the pre-gate blurred state AND the
post-gate unblurred state. Do not gate this label.

---

## Change 4 — Add Care-Months Reframe Below the Card

Directly below the single equity card (outside the card container),
add this line. It must be visible BOTH pre-gate and post-gate.
Do not blur or hide it.

Only render when `content.equityIllustration.length > 0` and
`result.ageBand !== 0`.

```tsx
<p className="text-[15px] text-stone-600 text-center leading-relaxed
  mt-4 max-w-prose mx-auto">
  This amount may cover 3 to 5 years of assisted living costs in
  Colorado -- without selling the property or touching savings.
</p>
```

---

## Change 5 — Add Proof Statement Above Gate Form

Directly above the gate form container, add:

```tsx
<p className="text-[14px] text-stone-500 text-center max-w-prose
  mx-auto mb-4 leading-relaxed">
  Dave spent a decade as an Executive Director running Assisted Living
  and Memory Care communities in Colorado. He has helped hundreds of
  families work through exactly this question.
</p>
```

---

## Change 6 — Simplify Gate Form Subtext

Find the gate form subtext. Replace its current text with:

"Takes 10 seconds. Your personalized funding summary goes straight
to your inbox."

Remove the mention of Dave's follow-up from this line entirely.
The P.S. line below the button already handles that.

---

## Change 7 — Add Status Line Post-Gate

In the post-gate render, find Zone B5 (salty pretzel section).
Add this paragraph AFTER the salty pretzel line and BEFORE Zone B6
(secondary phone ask):

```tsx
<p className="text-[15px] text-stone-500 text-center max-w-prose
  mx-auto mt-6 leading-relaxed">
  Families who understand the funding picture before they start
  placement conversations consistently make better decisions -- and
  feel better about them afterward. You have already done something
  most families skip entirely.
</p>
```

Only render for `likely-fit` and `explore-with-caveats` pathways.
Do not render for `medicaid-flag` or `not-a-fit`.

---

## Post-Gate Equity Cards — Show All Three, Highlight Theirs

In the post-gate state (Zone B1), keep showing all three equity cards
as currently implemented -- highlighted band prominent, others visible.
Only the pre-gate state shows a single card.

This gives context after submission (they can see how their situation
compares) while keeping the pre-gate experience focused and personal.

---

## Verification
- Run: `npx tsc --noEmit` -- zero errors required

Pre-gate checks:
- [ ] Only one equity card visible (matching result.ageBand)
- [ ] "Based on the age range you indicated:" appears above card
- [ ] "Estimated equity available:" label appears above blurred number
- [ ] Care-months reframe line visible below card
- [ ] Proof statement visible above gate form
- [ ] Gate form subtext is the simplified single-promise version

Post-gate checks:
- [ ] All three equity cards visible with ageBand highlighted
- [ ] "Estimated equity available:" label appears in each card
- [ ] Care-months reframe line still visible
- [ ] Status line renders for likely-fit and explore-with-caveats
- [ ] Status line does NOT render for medicaid-flag or not-a-fit

Track isolation:
- [ ] SNF track completely unchanged
- [ ] HCA track completely unchanged