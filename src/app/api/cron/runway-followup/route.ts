/**
 * Vercel Cron — runway follow-up email (stub only).
 * Follow-up sequence is deferred; no logic implemented.
 */

import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'runway follow-up sequence not yet implemented' });
}
