## SC TRACK -- FIX PROMPT B
## Task: Fix pronoun/verb interpolation on Q5 and any similar questions

CRITICAL -- DO NOT CHANGE:
- HCA, SNF, or RM track question components
- SC scoring engine or test suite
- Any Supabase wiring or email routes

---

PROBLEM:
Q5 is rendering "[Has/Have] she had any falls..." -- the verb form
[Has/Have] is not being resolved by the interpolate() function.
The pronoun "she" is resolving correctly but verb conjugation
is not handled.

FIX:
1. Read the current interpolate() function and pronoun map in
   the SC question component.

2. Add verb conjugation support to the interpolation map for
   each pronoun set established in Q1:

   "Myself" → Has (singular, second person treated as third)
   "My mother" / she → Has
   "My father" / he → Has
   "My spouse or partner" / they → Have
   "Another family member" / they → Have

3. Update Q5 question text so [Has/Have] resolves correctly
   based on Q1 answer.

4. Scan all 11 questions for any other unresolved interpolation
   tokens in the same [X/Y] format and fix them all in the
   same pass.

5. Confirm the fix by listing every question that contained
   an interpolation token and showing the resolved output
   for "My mother" as the Q1 answer.

DO NOT modify HCA, SNF, or RM question components.