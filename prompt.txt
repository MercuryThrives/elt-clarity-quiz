# Correction Run 2 — Copy Changes Only

## CRITICAL CONSTRAINT
Only modify `src/components/quiz/FundingRmResultsCard.tsx`.
Do not modify any other file.
Do not change any logic, structure, or styling.
Find and replace strings only.

---

## Read First
Read `src/components/quiz/FundingRmResultsCard.tsx` before making changes.

---

## Change 1 — Pre-gate headlines (by pathway)

The component renders a headline before the gate. Find the section that
renders the pre-gate headline and replace the strings for each pathway
with these exact values:

```typescript
// likely-fit:
"There may be real money here. Here's what to know first."

// explore-with-caveats:
"There may still be real money here. Here's what to know first."

// medicaid-flag:
"Before any reverse mortgage conversation happens, read this."

// not-a-fit:
"A standard reverse mortgage may not fit -- but the funding question isn't closed."
```

If the headline is currently pulled from `content.headline`, replace it
with a hardcoded switch/map using the strings above keyed by
`result.pathway`. Do not modify `funding-rm-content.ts`.

---

## Change 2 — Pre-gate tension paragraphs (by pathway)

Find the pre-gate tension paragraph. Replace the strings for each
pathway with these exact values:

```typescript
// likely-fit:
"Based on what you shared, the numbers may be more meaningful than your family
expects. Enter your email to see what they could look like."

// explore-with-caveats:
"Based on what you shared, there are factors worth understanding here.
At least one of them may matter less than you think. Enter your email
to see the full picture."

// medicaid-flag:
"Based on what you shared, Medicaid is part of this picture.
Getting the sequence right matters -- and getting it wrong can create
problems that didn't exist before."

// not-a-fit:
"Based on what you shared, a standard reverse mortgage may not be the
right tool. But there are other options most families never think to look at."
```

If the paragraph is currently pulled from `content.body`, replace it
with a hardcoded switch/map using the strings above keyed by
`result.pathway`. Do not modify `funding-rm-content.ts`.

---

## Change 3 — Add urgency line

Find the location between the blurred equity cards teaser text and the
gate form container. Insert this line there:

Render ONLY when `result.pathway === 'likely-fit'` or
`result.pathway === 'explore-with-caveats'`.

```tsx
<p className="text-[14px] text-stone-500 text-center mt-3 mb-6 leading-relaxed">
  Every month without a funding answer is a month of care costs paid
  from the wrong pocket.
</p>
```

Do not render for `medicaid-flag` or `not-a-fit`.

---

## Change 4 — Add salty pretzel line

Find the salty pretzel placeholder div with id="salty-pretzel" that
was added in Run 1. Replace it with this:

Render ONLY when `result.pathway === 'likely-fit'` or
`result.pathway === 'explore-with-caveats'`.

```tsx
<p className="text-[15px] text-stone-500 text-center max-w-prose mx-auto
  mt-10 leading-relaxed border-t border-stone-100 pt-8">
  Most families who look at the home equity question are also in the early
  stages of figuring out what kind of care actually fits their loved one's
  situation. That is a separate conversation -- and one Dave is glad to
  have whenever you are ready.
</p>
```

Do not render for `medicaid-flag` or `not-a-fit`.

---

## Verification
- Run: `npx tsc --noEmit` -- zero errors required
- Confirm likely-fit pathway shows "There may be real money here."
- Confirm explore-with-caveats shows "There may still be real money here."
- Confirm urgency line renders for likely-fit and explore-with-caveats only
- Confirm salty pretzel line renders for likely-fit and explore-with-caveats only
- Confirm no content changes to SNF or HCA tracks